# Core dependencies
streamlit>=1.32.0
torch>=2.2.0
transformers>=4.39.0
accelerate>=0.27.0
bitsandbytes>=0.43.0

# Optional but recommended (for stable inference and GPU optimization)
safetensors>=0.4.2
sentencepiece>=0.1.99
protobuf>=4.25.1

# For local fine-tuned model support
peft>=0.10.0

# (Optional) If using tokenizer fast implementations
tokenizers>=0.19.1

# (Optional) If using HF Hub login or uploading models
huggingface-hub>=0.24.0

